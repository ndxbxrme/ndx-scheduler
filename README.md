# ndx-scheduler

    creates ndx.scheduler

### public api

    task: (task) ->

### private api

    parseDate = (dateObj, task, start) ->
    insertScheduleItem = (date, data, taskId, cb) ->
    tick = ->

